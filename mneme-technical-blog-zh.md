---
title: "把本地文档变成可核验的记忆：Mneme 的 RAG / Graph RAG 工程实践"
description: "从混合检索、索引一致性到不可信文档边界，介绍 Mneme 如何把一个本地文档问答原型做成可维护的 RAG 系统。"
date: 2026-07-23
tags:
  - Python
  - RAG
  - Graph RAG
  - ChromaDB
  - TUI
lang: zh-CN
slug: mneme-rag-engineering-zh
---

# 把本地文档变成可核验的记忆：Mneme 的 RAG / Graph RAG 工程实践

很多 RAG 项目从一个很短的流程开始：把文档切成 chunk，生成 embedding，召回几段文本，再交给大语言模型回答。这个流程可以很快跑通，但真正让系统变得可靠的，往往不是“再换一个模型”，而是几个更基础的问题：文件修改后旧内容会不会残留？两个目录里同名文件会不会被混淆？回答能不能追溯到具体来源？文档中的恶意指令会不会被模型当成系统指令？

Mneme 是我围绕这些问题构建的本地文档 RAG 系统。它以 Mnemosyne——希腊神话中的记忆女神——命名，提供 Python CLI、双语终端 UI，以及 Standard RAG 和 Graph RAG 两种检索模式。本文不把它当作一个“功能清单”，而是从工程取舍出发，介绍它如何把本地文档问答拆成一条可验证的处理链路。

## 1. 先把 RAG 画成一条完整的数据链

Mneme 的核心路径可以概括为：

```text
本地文件
  → 路径规范化、类型与资源校验
  → 文档切分与稳定身份标记
  → ChromaDB 语义索引 + BM25 关键词索引
  → RRF 融合与动态 Top-K
  → PDF 锚点增强 / Graph RAG 扩展
  → 带来源和安全边界的上下文
  → LLM 回答与可核验引用
```

这里最重要的设计是：索引、检索、上下文构造和回答生成不是互相独立的模块。它们共享同一套来源身份和版本信息，最终生成的回答也必须能够回到具体的文件、页码和 chunk。

项目支持 PDF、DOCX、Markdown、HTML、JSON、CSV、XML、YAML、常见配置文件和多种源码文件。索引过程仍然在本地完成；但当 Mneme 进行查询拆解、Graph RAG 实体抽取或回答生成时，检索到的文本片段会发送到配置的 OpenAI 兼容端点。因此，“本地索引”不等于“数据永远不离开机器”，这条边界必须在产品和文档中说清楚。

## 2. 为什么选择混合检索

纯向量检索擅长理解语义相近的表达，但对文件名、缩写、数字、专有名词和精确短语并不总是稳定。纯 BM25 则容易错过同义改写。Mneme 同时维护两种检索结果：

- Sentence Transformers 生成 embedding，ChromaDB 负责语义召回；
- BM25 负责关键词召回，并复用未变化 chunk 的分词结果；
- RRF（Reciprocal Rank Fusion）根据排名融合两路结果，而不是强行比较不同检索器的原始分数。

RRF 的好处是简单、可解释，并且不要求语义距离和 BM25 分数处在同一量纲。融合后，Mneme 会根据结果分数的间隔选择动态 Top-K：如果候选结果在某个位置出现明显断层，就在断层处截断；如果没有明显断层，则遵守配置的上下限。

这套设计也保留了一个重要的可调空间：检索质量可以用 Recall@k、MRR 和 nDCG 等指标做回归，而不是只凭“感觉回答变好了”。仓库中已经提供基准数据和质量门禁接口，后续替换 chunk 策略、embedding 模型或融合参数时，可以把检索质量纳入测试。

## 3. Graph RAG 解决的是“关联”，不是“更大的 Top-K”

当问题涉及多个文档、人物、机构或概念之间的关系时，仅仅把 Top-K 调大，通常只会增加噪声。Mneme 的 Graph RAG 流程是：

1. 让 LLM 从 chunk 批量抽取实体；
2. 保存实体到 chunk 的映射；
3. 统计实体在不同 chunk 中的共现关系；
4. 用最小共现次数过滤偶然噪声，并限制每个 chunk 参与建边的实体数量；
5. 查询时抽取问题实体，沿图谱做一跳扩散；
6. 将图谱召回与标准混合检索结果按 `alpha` 融合。

这几个限制很关键。早期的全连接子图会让一个包含很多实体的 chunk 产生大量弱关系，图谱看起来很“丰富”，但检索反而更嘈杂。现在实体映射仍保留完整信息，建边则使用共现阈值和每 chunk 实体上限，从而把“实体出现在哪里”和“哪些实体应该连边”区分开。

如果图谱为空，Graph RAG 会退化为标准语义/混合检索，而不是让一次空图构建阻塞整个流程。实体抽取也使用当前配置的模型和端点；当 API Key 或 Base URL 在长生命周期 TUI 会话中改变时，Graph RAG 客户端会随配置指纹刷新。

## 4. RAG 最容易被低估的问题：索引一致性

向量库里最危险的 bug 往往不是“召回率低”，而是“召回了已经不存在的内容”。为此，Mneme 没有把文件 basename 或正文文本当作唯一身份，而是建立了一套共享身份模型：

- 规范化后的绝对路径用于生成 `source_id`；
- 文件内容使用 SHA-256 记录版本；
- 每个 chunk 有稳定的 `chunk_id`；
- 每个 collection 有带版本号的 manifest；
- ChromaDB、BM25 快照、Graph RAG 缓存和引用记录都围绕这套身份工作。

这样可以处理几个常见边界：两个目录中都有 `report.pdf`，正文完全相同但来源不同；一个文件被修改后只替换它自己的 chunks；删除文件时只删除精确来源，而不是删除所有同名文件；更新中途发生异常时，collection、manifest 和 BM25 快照一起回滚。

这也是为什么索引更新不只是一次 `upsert`。一个来源变更实际上需要同步多个状态：向量数据、关键词索引、manifest、图谱缓存和查询快照。把它们当成一个可恢复的 mutation，系统才不会出现“向量库已更新，但 manifest 仍是旧版本”的半成功状态。

## 5. 引用不仅是 UI 装饰，也是上下文协议的一部分

Mneme 为一次查询生成局部引用 ID，例如 `S1`、`S2`。引用记录包含来源路径、文件名、PDF 页码和 chunk ID；最终回答展示来源时，可以从回答中的 `[S1]` 回到具体证据。

更重要的是，检索到的正文会放在明确的不可信文档边界中：

```text
[Source: report.pdf] [Citation: S1]
<untrusted_document chunk_id="source-a_chunk_3">
检索到的正文……
</untrusted_document>
```

系统提示模型把这部分当作数据，而不是指令。文档里即使出现“忽略之前的指令”之类的文本，也应该停留在不可信文档区域内。

上下文长度受上限控制，默认最多向远程端点发送 60,000 个字符。这里不能简单地对整个字符串做截断：如果只剩一个开标签，模型看到的协议边界就不完整了。Mneme 的做法是只截断 `document_text`，完整保留来源、引用和开闭边界；如果连一个完整安全框架都放不下，就跳过这个 chunk。这是一个很小但很重要的工程细节：安全边界本身不能成为预算不足时最先被牺牲的内容。

## 6. 安全策略要覆盖文件、端点和缓存

Mneme 把安全校验集中在轻量的 `src/security.py`，让 CLI、TUI、查询拆解和 Graph RAG 使用同一套规则：

- 远程端点默认要求 HTTPS；`localhost`、`127.0.0.1` 和 `::1` 等回环地址允许 HTTP；
- 非本机 HTTP 必须显式设置 `MNEME_ALLOW_INSECURE_HTTP=1`；
- 可以限制单个文档大小、PDF 页数和允许的文档根目录；
- `.env` 被明确禁止建立索引；
- Graph RAG 缓存使用 schema 校验的原子 JSON，不加载 pickle；
- 缓存还要匹配当前索引 fingerprint 和 manifest version。

这些规则解决的是不同层面的问题：端点策略保护传输，资源上限防止意外的大输入，路径限制控制数据范围，JSON 缓存避免把可执行反序列化带进本地知识库，而 manifest 版本则防止旧图谱附着到新索引上。

## 7. TUI 让后台更新也变得可控

命令行适合脚本和一次性查询，Mneme 的 Rich TUI 则负责长期使用：流式回答、文件管理、设置、来源展示、Standard RAG / Graph RAG 切换，以及目录监控。

目录监控会对文件事件做防抖；真正的新增、修改、删除和重建操作进入单 worker 的 `IndexQueue` 串行处理。查询不会直接读取正在变动的列表，而是使用不可变快照固定本次查询看到的 collection、BM25、文档、metadata 和 manifest version。这样可以避免一个查询同时读到新 BM25、旧文档列表和另一版本的图谱。

这类设计牺牲了一点并发写入吞吐，却换来了更容易推理的状态模型。对于个人知识库和中小规模文档集，先把“不会半更新、不会静默失败、查询版本稳定”做好，通常比盲目追求写入并行度更有价值。

## 8. 测试的重点是边界，而不只是主流程

项目测试覆盖的不只是“输入一个文档能否回答问题”，还包括：

- 同名路径和重复正文是否保持独立身份；
- 修改、删除和 upsert 失败时 manifest 是否正确回滚；
- Graph JSON schema、大小、fingerprint 和版本是否有效；
- 低证据检索是否在调用 LLM 前拒答；
- 引用、PDF 页码和不可信文档边界是否完整；
- 索引队列是否串行，快照是否与后续 mutation 隔离；
- Windows/Linux 清理是否可重复，真实外部 LLM 测试是否通过 `MNEME_RUN_INTEGRATION=1` 显式开启。

默认测试套件保持离线安全；需要真实外部服务时再显式运行 integration 测试。这让本地开发和 CI 不必依赖个人 API 配置，也让“安全行为”可以用稳定的回归测试固定下来。

## 结语：可靠 RAG 的核心是把不确定性放在边界内

Mneme 的核心经验可以浓缩成一句话：RAG 的质量不只由召回模型决定，还由身份、版本、证据和安全边界共同决定。

混合检索负责扩大有效召回，Graph RAG 负责连接跨文档关系，manifest 负责让索引跟得上文件变化，引用和不可信文档边界负责让回答可追溯，端点与资源策略负责控制数据流向，而队列与快照负责让长期运行的 TUI 保持一致。

它仍然是一个本地优先的个人知识库工具，不是“数据绝不外发”的离线系统；如果文档敏感，仍需要配置可信的 LLM 端点，或使用受控的本地服务。也正因为如此，把数据边界写进架构，而不是只写在 README 里，才是这个项目最值得保留的工程实践。

项目入口和运行方式见 [README.zh.md](./README.zh.md)。
