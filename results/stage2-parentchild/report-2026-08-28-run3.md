# Stage 2 · 2.2 parent-child/邻接扩展 验收报告（第 3 轮终审：containment-aware 主指标）

- 日期：2026-08-28
- 预注册：`plans/STAGE2-PART2-DESIGN-2026-08-28.md` Round-3 节（owner 批准
  「批准」，2026-08-28）——**修仪器而非调阈值，阈值冻结不变**
  （Δ≥0.05、单例恶化≤0.05）
- 执行器：`evaluation/parentchild_ab.py`（`metric_version =
  r3-containment-aware`）；语料 6 文件 736 块；n=71（剔除无匹配真值 4）
- 密封产物：`run-3-2026-08-28/`（142 行 + 自哈希 manifest 复算 OK）

## 单变量保证：只换仪器，不换数据

run-3 复用 run-2 同一沙箱索引快照（不 force rebuild）：dataset_sha256
`8ce1b46b9eeaa543…` 与 corpus_files 逐项和 run-2 manifest 一致。两轮唯一
差异是主指标口径（chunk-id 集合交集 → containment-aware 真值匹配）。

## 指标定义（预注册冻结，先于重跑）

真值块计入覆盖，当且仅当满足其一：

1. **id 命中**：真值 chunk_id ∈ context chunk id 集（select 直接召回）；
2. **文本包含**：真值块文本（空白归一：连续空白折叠为单空格）是任一
   context 块文本（同归一）的连续子串；**空文本真值不适用**（空串是任何
   串的子串，显式排除）。

配套：真值/context 文本按 chunk_id 从同一索引快照反查并随密封产物落盘；
拒答计 0；真值 id/text 长度不一致 fail-closed；判定顺序与门禁三态不变。

## 判定：`STAGE2_22_ACCEPTED`（机器判定，2.2 `[~]→[x]`）

| 指标 | OFF | ON | Δ | worst_case |
| --- | --- | --- | --- | --- |
| **round-3 门禁**：containment-aware recall | 0.4377 | 0.5680 | **+0.1303** ✅（≥0.05） | **0.0** ✅（≥−0.05） |

71 例无一恶化——预算调和修复（run-2）消除了真挤占，containment 仪器
消除了 parent 替换的假性计 0，两者合并后扩展的净收益完整显形。

## 三轮轨迹（同一实验设计的演进）

| 轮次 | 主指标 | mean OFF | mean ON | Δ | worst | 判定 |
| --- | --- | --- | --- | --- | --- | --- |
| run-1 | chunk-id 交集 | 0.434 | 0.553 | +11.9pp | −1.0 | NOT_PROVEN（真挤占 3 例） |
| run-2 | chunk-id 交集 | 0.4269 | 0.5396 | +11.3pp | −0.5 | NOT_PROVEN（真挤占 0；失明 1 例） |
| **run-3** | **containment-aware** | 0.4377 | 0.5680 | **+13.0pp** | **0.0** | **ACCEPTED** |

round-2 containment-corrected 诊断（Δ=+0.1373 / worst=+0.00）与 run-3 正式
门禁结果一致（LLM 规划逐轮非确定，均值量级稳定复现）。

## en-017 复核（round-2 仪器失明案例）

- ON context = [chunk_13(parent), chunk_14(child), chunk_12(邻接)]，
  真值 = {chunk_13, chunk_15}；
- chunk_15（390 字符）全文包含于 chunk_13（parent，1277 字符）→
  containment 计覆盖 → **ON recall 1.0**（round-2 chunk-id 口径计 0.5）；
- OFF recall 1.0，en-017 Δ=0.0，假性恶化归零。

## 结论

1. **2.2 终审通过**：跨边界问答的 context recall 净收益成立
   （+13.0pp，零单例恶化），完成标准「在评测集 v1 上验证」满足；
2. 预算调和修复（`reconcile_expansion_budget`）作为行为改进独立保留；
3. 附带登记仍留产品线：邻接扩展最小块长守卫（4 字符碎块入 context）、
   parent 划分质量专项；
4. 方法论注记：本例验收史证明「预注册门禁 + 指标缺陷走预注册修订而非
   静默改判」的纪律有效——仪器失明被机械证据定位，owner 批准后以
   `metric_version` 区分口径重跑终审，历史判定（NOT_PROVEN）不被追溯改写。

## 数据卫生

沙箱运行零 trace 写入（sandbox 无 traces 目录，consent 缺省 → Off）；
v1/冻结树零改写；密封产物自哈希复算通过（outcomes + manifest）。
