# Mneme 项目今日变更审查报告

**日期**：2026-07-29  
**审查范围**：已提交提交 + 工作区未提交更改

---

## 一、变更概览

| 类型 | 数量 | 内容 |
|------|------|------|
| 已提交 | 1 条提交 | 删除 `docs/` 目录（VitePress 文档站点） |
| 未提交 | 4 个新文件 + 5 个修改文件 | Docker 容器化支持、`mneme` CLI 入口点、README/CHANGELOG 双语更新、技术博客草稿 |

---

## 二、已提交更改

### `692995b` — `chore: remove docs/ directory from repository`

- 删除整个 `docs/` 目录（VitePress 文档站点及相关资源）
- 提交信息规范，与前序提交（VitePress base 路径修复）形成完整的清理链条
- 无额外问题

---

## 三、未提交更改（工作区）

### 3.1 新增文件

#### `Dockerfile`

**设计亮点**：
- **多阶段构建**（builder + runtime）：构建阶段安装 gcc/g++ 编译依赖，运行阶段仅保留 `libgomp1`，有效减小镜像体积
- **预下载嵌入模型**：构建时通过 `modelscope` 下载 `all-MiniLM-L6-v2`，避免容器首次启动时的网络等待
- **离线模式**：设置 `HF_HUB_OFFLINE=1` 强制走 ModelScope 路径，符合项目国内友好的设计原则
- **环境变量覆盖**：`EMBEDDING_MODEL_PATH` 支持通过环境变量指定本地模型路径

**发现的问题**：

| 问题 | 严重级别 | 说明 |
|------|----------|------|
| 模型挂载覆盖风险 | 🔴 高 | `docker-compose.yml` 中 `./models:/app/models` 挂载会在宿主机 `./models` 为空时覆盖镜像中预下载的模型，导致 `EMBEDDING_MODEL_PATH` 指向不存在的路径，容器启动失败 |
| `pytest` 混入生产依赖 | 🟡 中 | `requirements.txt` 第 17 行包含 `pytest==9.0.3`，而 `pyproject.toml` 已将其正确放在 `[project.optional-dependencies] dev` 中，应保持一致 |
| 可编辑安装不够健壮 | 🟡 中 | `pip install -e .` 依赖绝对路径一致性，建议改用 `pip install --no-cache-dir .`（非 editable），简化运行时阶段 |

#### `docker-compose.yml`

**设计亮点**：
- 三种服务模式覆盖完整使用场景：
  - `tui`（默认）：`docker compose up` 启动终端 UI，正确配置 `tty` + `stdin_open` 交互
  - `rag`：`docker compose run rag --files /data/xxx` 标准 RAG CLI
  - `graph-rag`：`docker compose run graph-rag --files /data/xxx` Graph RAG CLI
- `env_file: .env` 简化环境变量传递，无需手动 `-e` 传递
- 数据持久化通过 volume 挂载：`./data`（文档）、`./chroma_db`（向量索引）、`./models`（模型缓存）

**注意**：`rag` 和 `graph-rag` 服务的 `entrypoint` 覆盖了 Dockerfile 中的 `ENTRYPOINT ["mneme"]`，这是正确且必要的设计，三种模式共用同一镜像，仅入口点不同。

#### `.dockerignore`

- 正确排除了 `.git`、Python 虚拟环境、运行时数据（`chroma_db/`、`models/`、`data/`）、敏感信息（`.env`）、IDE 文件、测试相关文件等
- 已包含 `.playwright-mcp/`，合理

#### `mneme-technical-blog-en.md` / `mneme-technical-blog-zh.md`

- 两篇技术博客草稿，尚未提交，属于未完成内容

---

### 3.2 修改文件

#### `pyproject.toml`

- 新增 `[project.scripts]` 段，注册 `mneme = "tui.__main__:main"` console_scripts 入口点
- 安装后可直接运行 `mneme` 启动 TUI，替代原有的 `python -m tui`
- 符合 PEP 621 规范

#### `tui/__main__.py`

- 从直接执行 `RagApp().run()` 改为提取 `main()` 函数 + `if __name__ == "__main__"` 保护
- 添加 docstring 说明作为 `console_scripts` 入口点的用途
- 与 `pyproject.toml` 的改动配合正确

#### `README.md` / `README.zh.md`

- 双语同步更新，新增 **"Option A: Docker（推荐）"** / **"方式 A：Docker（推荐）"** 安装说明
- 启动命令从 `python -m tui` 更新为 `mneme`
- Docker 使用示例完整，包含数据持久化说明
- 原有的本地 Python 安装方式降级为 "Option B" / "方式 B"

#### `CHANGELOG.md`

- 详细记录了 Docker 支持的各个子项：
  - `Dockerfile`：多阶段构建、预下载模型
  - `docker-compose.yml`：三种服务模式
  - `.dockerignore`：排除规则
  - 数据持久化：`./data`、`./chroma_db`、`./models` volume 挂载
  - 环境变量通过 `.env` 文件自动注入
  - README 双语更新
- 详细记录了 `mneme` console_scripts 的变更：
  - `pyproject.toml` 新增 `[project.scripts]`
  - `tui/__main__.py` 提取 `main()` 函数
  - `Dockerfile` `ENTRYPOINT` 改为 `mneme`
- 格式符合 [Keep a Changelog](https://keepachangelog.com/) 规范，分类为 `Added` 和 `Removed`

---

## 四、其他发现

### `.gitignore` 缺少 `.playwright-mcp/`

- `.dockerignore` 已正确排除 `.playwright-mcp/`，但 `.gitignore` 中未添加
- 导致 `git status` 中显示大量 `.playwright-mcp/` 日志和快照文件为未跟踪状态（约 20+ 个文件）
- **建议**：在 `.gitignore` 末尾添加 `.playwright-mcp/`

---

## 五、综合评价

| 维度 | 评分 | 评价 |
|------|:----:|------|
| 功能设计 | ⭐⭐⭐⭐⭐ | Docker 三种服务模式覆盖完整，`mneme` CLI 简化启动，多阶段构建设计合理 |
| 代码质量 | ⭐⭐⭐⭐ | 整体良好，但存在模型挂载覆盖风险、pytest 混入生产依赖、可编辑安装不够健壮等问题 |
| 文档同步 | ⭐⭐⭐⭐⭐ | README 双语同步更新，CHANGELOG 详细记录了所有变更项 |
| 安全性 | ⭐⭐⭐⭐ | `.env` 正确排除在镜像外，敏感信息不进入 Docker 构建上下文 |

---

## 六、建议修复清单（按优先级排序）

| 优先级 | 问题 | 建议修复方案 |
|:------:|------|-------------|
| 🔴 高 | `docker-compose.yml` 中 `./models:/app/models` 挂载覆盖预下载模型 | **方案 A（推荐）**：删除 docker-compose 中的 `./models:/app/models` 挂载。模型是只读的且体积不大（约几十 MB），无需持久化；用户如需自定义模型，可通过环境变量覆盖。或 **方案 B**：编写 entrypoint 脚本，在启动时检查 `/app/models` 是否为空，若空则从镜像中的备份路径复制模型 |
| 🟡 中 | `requirements.txt` 包含 `pytest==9.0.3` | 从 `requirements.txt` 中移除 `pytest`，保持与 `pyproject.toml` 的 `[project.optional-dependencies] dev` 一致。生产环境不需要测试依赖 |
| 🟡 中 | Dockerfile 中使用 `pip install -e .` | 改为 `pip install --no-cache-dir .`（非 editable 安装），运行时阶段只需复制 `site-packages` 和 `bin`，无需单独复制 `src/`、`tui/`、`pyproject.toml`，减少层数且更健壮 |
| 🟢 低 | `.gitignore` 缺少 `.playwright-mcp/` | 在 `.gitignore` 末尾添加 `.playwright-mcp/`，避免日志和快照文件污染工作区 |

---

## 七、结论

今日变更整体质量较高，核心功能（Docker 容器化 + `mneme` CLI）设计合理，文档同步完善。主要风险在于 `docker-compose.yml` 中的模型 volume 挂载可能导致预下载模型在首次运行时被空目录覆盖，建议在合并前修复。
