# M5b 阶段 1：G2' 语义拒答正确率判据锚定（呈批稿）

- 日期：2026-09-06
- 状态：**待 owner 锁定**
- 上游：M5b 设计 §二阶段 1（owner 已批 Q-M5b-1/2/3）
- 关联判据：M5 提案 G1（修正率 ≥ 0.55，臂2 已满足 0.5575）+ 本文件 G2'

---

## 一、G2' 定义（口径）

**G2' = 语义拒答正确率（al3-refusal-judge-v1.1 口径）**：
对 28 条拒答探针（`should_refuse=True`），`semantic_verdict == "refused"` 的
比例；契约错误行按「未正确拒答」计入（保守，与 al3 聚合口径一致）。

## 二、既成事实（al3 读数，如实披露时序）

### 2.1 词表口径 vs 语义口径（v1.1 最终读数）

| 基线 | 语义口径（al3 v1.1） | 词表口径 | 偏差例 | 核查结论 |
| --- | ---: | ---: | ---: | --- |
| M2 | **1.0**（28/28） | 0.3929 | 0 | — |
| 臂1 | 0.9286（26/28） | 0.25 | 2 | noanswer-054 真实失败（幻觉式肯定）+ noanswer-042 边界歧义 |
| 臂2 | **1.0**（28/28） | 0.1786 | 0 | — |

**关键事实**：M2 与臂2 的探针答案语义上 **28/28 全部正确拒答**——此前所有
「拒答正确率 0.39/0.25/0.18」均为词表口径的测量伪影。真实的产品级拒答失效
仅 1 例（臂1 的 noanswer-054；臂2 已修复）。

### 2.2 al3 协议迭代（如实披露）

- v1：三基线读数 M2=1.0 / 臂1=0.8929 / 臂2=0.9286，人工核查发现 2/28
  系统性误判（noanswer-038/042「否定 + 解释该否定」型答案被误判 answered）；
- v1.1：增补该判定条款后重跑 → M2=1.0 / 臂1=0.9286 / 臂2=1.0；
  残余分歧仅臂1 noanswer-042（边界歧义，如实保留）；
- 本锚定文档的推荐值（0.90）**起草于 v1 读数产生之前**（依据人工核验的
  臂2 28/28 事实），两轮读数返回后均未修改。

### 2.3 裁决稳健性（对锚定档不敏感）

臂2 的 G2' = **1.0**，高于全部候选锚定档（0.85 / 0.90 / 0.96）——
**M5d 裁决结论在三档下一致**，锚定档选择不改变 PASS/FAIL 判定。

## 三、锚定数值（候选，待 owner 选定其一）

| 候选 | 数值 | 依据 |
| --- | ---: | --- |
| **A（推荐）** | **≥ 0.90** | M2 既成事实 1.0 的守卫折扣（允许 ≤2 例偏差）——拒答面板是「不该答」的守卫，基线既然已近乎完美，修复后不应显著退化 |
| B | ≥ 0.85 | 更宽松（允许 ≤4 例偏差）；臂2 v1.1 预期读数附近 |
| C | ≥ 0.96 | 更严（允许 ≤1 例偏差）——要求臂级修复不留任何新增拒答失效 |

**数值来源声明**：候选档均围绕 **M2 = 1.0 的既成事实**（该事实由 al3 v1 读数
确立并经人工核查确认 0 误判），不含对臂2 待观测数字的拟合。

## 四、判据组合与裁决规则（M5d）

- **PASS** ⇔ `G1 ≥ 0.55` **且** `G2' ≥ 锚定档`（两判据互不抵扣）；
- **FAIL** ⇒ 按 M5 §二（M4 §七同流程）：归档病理 + owner 重决策；
- 判据对象：**臂2 密封产物**（M5c 零生成方案）——G1 取 0.5575；G2' 取
  al3 v1.1 对同产物的重测量值。

## 五、待 owner 决策

- **Q-M5b-4**：G2' 锚定档选择（推荐 A = 0.90）。
